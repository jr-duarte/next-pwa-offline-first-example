import { styled } from '@Styles/theme/stitches.config';

const Wrapper = styled('section', {
  paddingTop: '$xxhuge',
  paddingBottom: '$xxhuge',
  '@lgUp': {
    paddingTop: '$xxhuge',
    paddingBottom: '$xxhuge',
  },
});

export default Wrapper;
